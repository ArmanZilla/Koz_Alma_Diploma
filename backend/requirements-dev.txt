# Development / testing dependencies.
# Install on top of production requirements:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# ── Testing ──
pytest>=7.4.0
httpx>=0.25.0
pytest-asyncio>=0.23.0
fakeredis[json]>=2.20.0

# ── Linting ──
ruff>=0.2.0
